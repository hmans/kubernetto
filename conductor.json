{
  "scripts": {
    "setup": "mise trust && mise install && mise x -- pnpm install --frozen-lockfile",
    "run": "exec ./scripts/dev-watch --port \"$CONDUCTOR_PORT\""
  },
  "runScriptMode": "concurrent"
}
